apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "endpoint.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "endpoint.name" . }}
    helm.sh/chart: {{ include "endpoint.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "endpoint.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
